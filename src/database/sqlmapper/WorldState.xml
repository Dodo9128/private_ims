<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fdreplay.ims.country.WorldState">
    <!--=======================================
    	목록 조회
     =======================================-->
	<sql id="listWorldStateFromWhere">
		FROM 	world_state WS
		WHERE	1=1
		<if	test='id !=null and id !=""'>
			AND WS.id = #{id}
		</if>
		<if	test='countryId !=null and countryId !=""'>
			AND WS.country_id = #{countryId}
		</if>
	</sql>
	
	<select id="listWorldStateCount" parameterType="Map" resultType="Integer">
		<include refid="CommonSQL.selectCount" />
		<include refid="listWorldStateFromWhere" />
    </select>

    <select id="listWorldState" resultType="Map">
		<include refid="CommonSQL.pagingPreFix" />
		SELECT 	
			WS.id
			, WS.name
			, WS.country_id
			, WS.country_code
			, WS.fips_code
			, WS.iso2
			, WS.flag
			, WS.wiki_data_id
		<include refid="listWorldStateFromWhere" />
		<include refid="CommonSQL.orderby" />
		<include refid="CommonSQL.pagingPostFix" />
    </select>
</mapper>